const MoviesDetails = ({movie}) => {
    const {
        backdrop_path, original_title, id, overview, release_date, genre_ids, poster_path, vote_average, vote_count
    } = movie;
    return (
        <div>


            
        </div>
    );
};

export {MoviesDetails};